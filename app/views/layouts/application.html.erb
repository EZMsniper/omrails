<!DOCTYPE html>
<html>
  <head>
    <title>Ben's One Month Pic Sharing Site</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <!-- Navigation Bar -->
    <nav class="navbar navbar-toggleable-sm navbar-inverse bg-inverse">
      <div class="container">
      <!-- Brand -->
        <%= link_to "Ben's Web App", root_path, class: "navbar-brand" %>
        <ul class="navbar-nav">
      <!-- Links -->
          <li class="nav-item"><%= link_to "Home", root_path, class: "btn" %></li>
          <li class="nav-item "><%= link_to "About", about_path, class: "btn" %></li>
          <li class="nav-item"><%= link_to "Contact", "lets-chat", class: "btn" %></li>
        <% if user_signed_in? %>
          <li class="nav-item pull-right"><%= link_to "Tweets", tweets_path, class: "btn btn" %></li>/
          <li class="nav-item pull-right"><%= link_to "Log out", signout_path, method: :delete, class: "btn btn-secondary" %></li>
        <% else%>
          <li class="nav-item"><%= link_to "Log in", signin_path, class: "btn btn-secondary.btn-sm" %></li>
          <li class="nav-item"><%= link_to "Sign Up", signup_path, class: "btn btn-primary" %></li>
        <% end %>
        <% if current_user.try(:admin?) %>
          <li class="nav-item pull-right"><%= link_to "Admin Panel", admin_dashboard_path, class: "btn btn float-right" %></li>/
        <% end %>
        </ul>
    </nav>

<!-- Flash Messages -->
  <% flash.each do |name, message| %>
     <%= content_tag(:div, message, class: "alert alert-info #{name}") %>
<% end -%>

    <!-- Page Content -->
    <%= yield %>
    
    <!-- Footer -->
    <footer class="small text-muted">
      <div class="container">
        <hr>
        <center>
          <p>© 2016 EZMSniper</p>
        </center>
      </div>
    </footer>

  </body>
</html>
